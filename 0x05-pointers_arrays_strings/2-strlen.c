#include "main.h"
/**
 * _strlen - Function prototype
 * Description: Returns the length of a string
 * @s: pointer of type char
 * Return: Always 0
 * **/
int _strlen(char *s)
{
	int x;

	for (x = 0; s[x] != '\0'; x++)
	{
		continue;
	}
	return (x);
}
